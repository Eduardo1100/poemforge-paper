# Porting Notes

## First milestone

Implement `pipeline/00_prepare_data.py`.

Required outputs:

- `data/processed/items.csv`
- `data/processed/targets_long.csv`
- `data/processed/targets_wide.csv`

Initial source candidates from the Phase A repo should include:

- `eval/poemset/eval_items_all_text.csv`
- `eval/poemset/eval_targets_all_text_long.csv`
- `eval/poemset/eval_targets_per_dimension_wide.csv`

The first pass may copy these frozen normalized files into the paper repo. The second pass should regenerate them from raw sources if feasible.

## Rule

No manuscript number is trusted until it is regenerated by the clean pipeline.
